import type { IDraggable } from '@/baseUi/draggable'

export const editDraggableConfig: IDraggable = {
  draggableProp: {
    group: { name: 'library' },
    itemKey: 'id',
    disabled: false,
  },
}
