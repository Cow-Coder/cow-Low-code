import type { IDraggable } from '@/base-ui/draggable'

export const editDraggableConfig: IDraggable = {
  draggableProp: {
    group: { name: 'library' },
    itemKey: 'id',
    disabled: false,
  },
}
